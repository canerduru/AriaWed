# Frontend (Vite). Point to backend when running API locally.
VITE_API_URL=http://localhost:3001

# Backend (server). Copy to .env in project root or server folder.
PORT=3001
JWT_SECRET=your-secret-change-in-production
GEMINI_API_KEY=your-gemini-api-key
SQLITE_PATH=./server/db/ariawed.db
